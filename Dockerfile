# Используем официальный образ Node.js как базовый образ
FROM node

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# кэшируем предварительно зависимости чтобы при новой сборке не тратить время
COPY package.json /app


# устанавливает npm если он будет то он закешируется
RUN npm install


# Копируем все файлы из вашего проекта внутрь контейнера
COPY . .


EXPOSE 3000

# Команда для запуска Vite Development Server
CMD ["npm", "run", "dev"]
